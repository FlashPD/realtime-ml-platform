name: tripml
version: "1.0.0"
config-version: 2

profile: tripml
model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  tripml:
    staging:
      +materialized: view
